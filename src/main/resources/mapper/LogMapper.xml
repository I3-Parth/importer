<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.parth.importer.repository.LogRepository">
    <resultMap id="LogResultMap" type="com.parth.importer.model.LogEntity" >
        <id property="id" column="id" />
        <result property="name" column="name" />
        <result property="age" column="age" />
        <result property="email" column="email" />
        <result property="city" column="city" />
        <result property="statusCode" column="status_code" />
        <result property="statusMessage" column="status_message" />
    </resultMap>

    <select id="getAllLogs" resultMap="LogResultMap" >
        select * from logEntity
    </select>

    <insert id="addLog" parameterType="com.parth.importer.model.LogEntity" keyColumn="id" keyProperty="id" useGeneratedKeys="true" >
        insert into logEntity (name, age, email, city, status_code, status_message)
        values (#{name}, #{age}, #{email}, #{city}, #{statusCode}, #{statusMessage})
    </insert>

    <update id="updateLog" parameterType="map" >
        update logEntity SET status_code = #{statusCode}, status_message = #{statusMessage} WHERE id = #{logId}
    </update>
</mapper>